import React from 'react';
import s from "./currentDialog.css"

const currentDialog = (props) => {
    return (
        <div className="wrapper">
            dsd
        </div>
    );
}

export default currentDialog;